service athinfo
{
        socket_type     = stream
        protocol        = tcp
        wait            = no
        user            = root
        server          = {% if python_alternative == "python2" -%}
                            /bin/athinfod
                          {% else -%}
                            /usr/local/bin/athinfod
                          {% endif %}
        port            = 49155
}
